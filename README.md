# Node LTS SDK

Node.js 24.19.0 for cpak development environments.

The image exports `node`, `npm`, `npx`, and `corepack` for amd64 and arm64. The Node archive is checked against the official SHA-256 before it is copied into the final image.

```sh
cpak install github.com/containerpak/sdk-node-lts
```
